apiVersion: v1
kind: Service
metadata:
  name: fhk-security-server
  namespace: prod
  labels:
    app: fhk-security-server
    env: prod
spec:
  type: ClusterIP
  selector:
    app: fhk-security-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
